apiVersion: v1
kind: Service
metadata:
  name: {{ include "ghcp-usage-dashboard.fullname" . }}
  labels:
    {{- include "ghcp-usage-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ghcp-usage-dashboard.selectorLabels" . | nindent 4 }}
